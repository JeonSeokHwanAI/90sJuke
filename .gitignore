# 의존성 모듈 (용량 큼, 설치하면 됨)
node_modules/
/node_modules

# Next.js 빌드 결과물 및 캐시 (절대 올리면 안 됨)
/.next/
.next/
out/
build/
dist/

# 환경변수 (보안상 절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS 시스템 파일
.DS_Store
Thumbs.db# 의존성 모듈 (용량 큼, 설치하면 됨)
node_modules/
/node_modules

# Next.js 빌드 결과물 및 캐시 (절대 올리면 안 됨)
/.next/
.next/
out/
build/
dist/

# 환경변수 (보안상 절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS 시스템 파일
.DS_Store
Thumbs.db